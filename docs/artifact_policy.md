# Artifact policy

The repository stores the finite certificate records required for deterministic public verification of the certified threshold `0.833` in the convex Brass-Sharifi three-test-set certificate model.

The public commands replay bundled records; they do not rerun the original geometric search and do not regenerate the certificate from scratch.
