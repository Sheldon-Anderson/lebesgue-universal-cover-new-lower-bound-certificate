# Release audit

The release audit verifies that the certificate manifest, hashes, final adjudication gate, final aggregation gate, and claim boundary are mutually consistent.

Expected release status:

- `theorem_ready = true`
- `final_blocker_count = 0`
- `claim_boundary_ok = true`
